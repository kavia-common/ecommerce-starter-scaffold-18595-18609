# Preferred API base URL env vars for the frontend container.
# The app will try these in order:
# - NG_APP_API_BASE
# - NG_APP_BACKEND_URL
#
# Default fallback if absent: http://localhost:3001
NG_APP_API_BASE=http://localhost:3001
